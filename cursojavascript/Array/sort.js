const vendedores = ["Maria","Ana","Ronyevon", "Raquel","Madruga"];
const sorteio = vendedores [Math.floor(Math.random() * vendedores.length)];
console.log (sorteio);

// push serve para adicionar um elemento no fim do vetor
//unshift serve para adicionar no inicio do vetor
//pop exclui o  ultimo elemento
// shift remove elementos no incio do vetor
// slice mostrar todos os elementos que estao em um determinado intervalo